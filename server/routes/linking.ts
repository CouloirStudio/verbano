import { Router } from "express";
const router = Router();

router.get("/", (req, res) => {
  // Logic for getting all links
});

router.post("/", (req, res) => {
  // Logic for creating a new link
});

router.delete("/", (req, res) => {
  // Logic for deleting an existing link
});

export default router;
