query GetNote($id: ID!) {
    getNote(id: $id) {
        id
        audioLocation
        dateCreated
        transcription
        summary
        tags
        noteName
        noteDescription
    }
}
